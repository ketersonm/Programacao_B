Vetor vetorTMP
faça TMP para temporario para facilitar


   alunos.push_back(perfilTMP);
alunos eh o nome 2 do vetor.


if (casas.empty()) {
casa eh o nome 2 do vetor, eh empty() eh a mesma coisa que casas.size() == 0.



Strunct - Recurso antgo para criar TIPOS do programador

Typedef struct {
    float peso;
    float altura;
    string nome;
} IndiceMassa;

main (){
vector<IndiceMassa> pessoas;
float peso = 74.3
float altura = 1.85
string nome = "Bla"
pessoas.push_back({peso, altura, mome})

variavel transitoria. para validaçao
    




https://dontpad.com/algoritmosufn

printf -> cout
scanf -> cin
Todo vetor eh um ponteiro
fflush -> limpa o buffer
vector<Tipo> vetor
vetor.size() -> mede o tamanho
pushback() inserir
getline(cin, telefone) para pegar a linha inteira, junto do espaço
using namespace std;
typedef struct --> para fazer os itens separado

control  + l limpa a tela
shift alt f 


Apresentação da disciplina e revisão

.\roda

.h termina assim apenas em C.

Como rodar com o terminal em C:  
cd Desktop
PS C:\Users\laboratorio\Desktop> (g++ apenas em c++) g++ eutu.cpp  -o eutu.exe
PS C:\Users\laboratorio\Desktop> gcc prog1.c -o prog1.exe
PS C:\Users\laboratorio\Desktop> .\prog1.exe

cin, em C++ usa apenas o primeiro.
getline pega tudo.

nome.length() conta quantos caracteres tem.
Nome repositorio: algoritmos
